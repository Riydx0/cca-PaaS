kind = "web"
previewPath = "/"
title = "Cloud Services Marketplace"
version = "1.0.0"
id = "artifacts/cloud-marketplace"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21508

[services.development]
run = "pnpm --filter @workspace/cloud-marketplace run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/cloud-marketplace", "run", "build" ]
publicDir = "artifacts/cloud-marketplace/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21508"
BASE_PATH = "/"
